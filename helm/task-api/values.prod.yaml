replicaCount: 2

env:
  APP_ENV: "prod"

image:
  repository: task-api
  tag: stable
  pullPolicy: IfNotPresent

secret:
  create: true
  existingSecret: ""

ingress:
  enabled: true
  className: ""
  hosts:
    - host: task-api.example.com
      paths:
        - path: /
          pathType: Prefix

livenessProbe:
  httpGet:
    path: /healthz
    port: http

readinessProbe:
  httpGet:
    path: /healthz
    port: http

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
